#include "lib.h"

int main(){
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    int tc;
    std::cin >> tc;
    while(tc--){
        int N,Q;
        std::cin >> N >> Q;
        std::vector<int> arr(N);
        for(int i=0;i<N;i++){
            std::cin >> arr[i];
        }
    }
    return 0;
}